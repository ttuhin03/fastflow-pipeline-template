{
  "description": "Pipeline using alternative JSON filename (pipeline_b.json). Triggers pipeline_c on success.",
  "tags": ["example", "custom-json"],
  "enabled": true,
  "webhook_key": "b-secret-webhook-key",
  "downstream_triggers": [
    {"pipeline": "pipeline_c", "on_success": true, "on_failure": false}
  ]
}
