{
  "description": "Upstream pipeline – triggers chaining_downstream on success.",
  "tags": ["example", "chaining"],
  "downstream_triggers": [
    {
      "pipeline": "chaining_downstream",
      "on_success": true,
      "on_failure": false
    }
  ]
}
