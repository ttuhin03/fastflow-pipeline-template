{
  "description": "Downstream pipeline – triggered by chaining_upstream on success. Optional schedules: trigger can specify run_config_id to run this config.",
  "tags": ["example", "chaining"],
  "schedules": [
    { "id": "prod", "schedule_cron": "0 9 * * *", "enabled": true }
  ]
}
